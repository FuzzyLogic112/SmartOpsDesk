<Window x:Class="SmartOpsDesk.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartOpsDesk 登录"
        Width="420"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F8"
        FontFamily="Microsoft YaHei UI">
    <Border Background="White" CornerRadius="8" Padding="24" Margin="22" BorderBrush="#E1E7EF" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="SmartOpsDesk" FontSize="26" FontWeight="Bold" />
            <TextBlock Text="选择演示账号登录，不同角色拥有不同操作权限。" Foreground="#667085" TextWrapping="Wrap" Margin="0,6,0,18" />

            <TextBlock Text="账号" />
            <ComboBox x:Name="UserNameBox" Padding="8" Margin="0,4,0,12">
                <ComboBoxItem Content="dev" IsSelected="True" />
                <ComboBoxItem Content="pm" />
                <ComboBoxItem Content="impl" />
                <ComboBoxItem Content="ops" />
            </ComboBox>

            <TextBlock Text="密码" />
            <PasswordBox x:Name="PasswordBox" Padding="8" Margin="0,4,0,12" Password="123456" />

            <TextBlock Text="演示账号：dev/pm/impl/ops，密码均为 123456。" Foreground="#667085" Margin="0,0,0,16" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="登录" Width="96" Padding="12,8" Background="#155EEF" Foreground="White" BorderBrush="#155EEF" Click="Login_Click" />
                <Button Content="退出" Width="96" Padding="12,8" Margin="8,0,0,0" Click="Cancel_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
